{
  "name": "seacloud-mirror-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "海云镜像 SeaCloud Mirror — 多源 Windows 原版镜像聚合查询前台 (Vue3 + Vite + Tailwind)",
  "scripts": {
    "prepare:data": "node scripts/prepare-data.mjs",
    "predev": "npm run prepare:data",
    "dev": "vite",
    "prebuild": "npm run prepare:data",
    "build": "npm run prepare:data && vite build && node scripts/generate-static.mjs",
    "build:browser": "npm run prepare:data && PRERENDER=browser vite build",
    "preview": "node scripts/serve-static.mjs"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "vite": "^5.4.11",
    "vite-plugin-prerender": "^1.0.8"
  },
  "engines": {
    "node": ">=18"
  }
}
